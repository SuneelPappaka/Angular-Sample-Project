export interface HostelModel {
    hostelId: number;
    hostelName: string;
    hostelType: string;
    address: string;
    totalRooms: number;
    wardenId: number;
}

// ng g interface models/hostel.model
